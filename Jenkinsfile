pipeline {
    agent {
        docker { image 'ubuntu:20.04'}
    }
    stages {
        stage ('build') {
            steps {
                sh 'ubuntu --version'
                sh 'pwd'
            }
            
        }
    }
}